name = "game"
max_entries = 200000
include_forms = true
include_multiword_terms = false
include_proper_nouns = false
enable_wordnet = true
enable_frequency = true
# Embeddings remain opt-in until a local model provider is configured.
enable_embeddings = false
max_relations_per_token = 48
